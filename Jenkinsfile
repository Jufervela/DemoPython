pipeline
{
    agent any
    stages {
        stage('git checkout') {
            steps {
                git branch: 'main', url: 'https://github.com/Jufervela/DemoPython.git'
            }
        }

        stage('Unit testing') {
            steps {
                
            }
        }
    }
}
